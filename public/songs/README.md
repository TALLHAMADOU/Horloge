# Sleep Songs

Drop your audio files in this folder, then list them in `playlist.json`.

Example:

```json
{
  "tracks": ["rain.mp3", "ambient-night.ogg", "slow-piano.wav"]
}
```

The app plays one track at random when the timer starts. Track names are never displayed in the interface.
